# Migrations (local stubs)

This folder contains local Alembic-style migration stubs for development.

It is not a full Alembic environment. Use these as migration templates and run `alembic revision --autogenerate -m "create competitor_prices"` in a proper Alembic environment for production.
